import styled from "styled-components";

const ImageWrapper = styled.div`
  display: flex;
  justify-content: end;
  align-items: center;
  cursor: zoom-in;
  img {
    max-width: 100px;
    max-height: 150px;
  }
`;

export { ImageWrapper };
